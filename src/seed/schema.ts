export interface SeedLazyModuleOptions {
    name: string;
    appRoot: string;
    path: string;
    sourceDir: string;

    project: string;

    // module: string;
    // export: boolean;

    // model: string;
}